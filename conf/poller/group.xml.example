<?xml version="1.0"?>
<!--

A group config file contains one outer group element containing unlimited mib elements

The group element has 3 required attributes:
    - "workers" (The number of workers that should poll for this group)
    - "interval" (The period in seconds that the group should be polled)
    - "timeout" (The time to wait for a response before timing out)

Additional attributes can be given for the group element:
    - "request_size" (Indicates how many results to send back per SNMP request, default in main config)

mib elements have onve attribute and no contents:
    - "oid" (An oid that should be polled for hosts belonging to the group)


Example File Contents:

<group workers="1" interval="60" timeout="30">
    <mib oid="1.3.6.1.2.1.2.2.1.1"/>
    <mib oid="1.3.6.1.2.1.31.1.1.1.1"/>
    <mib oid="1.3.6.1.4.1.2636.3.60.1.1.1.1"/>
    <mib oid="1.3.6.1.4.1.2636.3.60.1.2.1.1"/>
</group>

-->

<group workers="" interval="" timeout="">
    <mib oid=""/>
</group>
