<?xml version="1.0"?>

<!-- Main Simp Data Config -->
<config workers="4">

    <!-- Redis Configuration -->
    <redis    ip="127.0.0.1" port="6379" reconnect="60" reconnect_every="500" read_timeout="2" write_timeout="3" />

    <!-- RabbitMQ Configuration -->
    <rabbitmq ip="127.0.0.1" port="5672" user="guest" password="guest" />

</config>
